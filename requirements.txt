playwright==1.49.1
beautifulsoup4==4.12.3
lxml==5.3.0
trafilatura==1.12.2
pypdf==5.1.0
tqdm==4.67.1
langdetect==1.0.9
urllib3==2.2.3

# RAG baseline
numpy
rank-bm25
sentence-transformers
# 4.45.x is the last release that has DynamicCache.from_legacy_cache (removed in 4.46),
# tokenization_qwen2_fast at its original module path (reorganised in 5.x), and
# Qwen2Config.rope_theta as a direct attribute — all required by stella's
# trust_remote_code files. 4.45 also introduced first-class Qwen2.5 support.
transformers==4.45.2
accelerate
torch>=2.0.0
sentencepiece

# FAISS for fast dense retrieval (used by build_index.py / run_rag.py)
# faiss-gpu is abandoned on PyPI and only supports Python <3.11.
# Use faiss-cpu (works everywhere) or faiss-gpu-cu12 on CUDA 12 machines.
faiss-gpu-cu12

# BAAI/bge-m3 embedding model support (pin to avoid API breaks)
FlagEmbedding==1.2.10